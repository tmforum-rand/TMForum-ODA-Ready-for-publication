apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{.Release.Name}}-svcqualityapi
  labels:
    oda.tmforum.org/componentName: {{.Release.Name}}-{{.Values.component.name}}
spec:
  replicas: 1
  selector:
    matchLabels:
      impl: {{.Release.Name}}-svcqualityapi
  template:
    metadata:
      labels:
        app: {{.Release.Name}}-{{.Values.component.name}}
        impl: {{.Release.Name}}-svcqualityapi
        version: {{.Values.servicequalityapi.versionLabel}}
    spec:
      containers:
      - name: {{.Release.Name}}-svcqualityapi
        image: {{.Values.servicequalityapi.image}}
        imagePullPolicy: {{.Values.image.pullPolicy}}
        env:
        - name: RELEASE_NAME
          value: {{.Release.Name}}
        - name: COMPONENT_NAME
          value: {{.Release.Name}}-{{.Values.component.name}}
        - name: PORT
          value: "8080"
        - name: OTL_EXPORTER_CONSOLE_ENABLED
          value: "{{.Values.servicequalityapi.otlp.console.enabled}}"
        - name: OTL_EXPORTER_TRACE_PROTO_ENABLED
          value: "{{.Values.servicequalityapi.otlp.protobuffCollector.enabled}}"
        - name: OTL_EXPORTER_TRACE_PROTO_COLLECTOR_URL
          value: {{.Values.servicequalityapi.otlp.protobuffCollector.url}}
        - name: MONGODB_HOST
          value: {{.Release.Name}}-mongodb
        - name: MONGODB_PORT
          value: "{{.Values.mongodb.port}}"
        - name: MONGODB_DATABASE
          value: {{.Values.mongodb.database}}
        - name: NODE_ENV
          value: production
        - name: CANVAS_INFO_HOST_PORT
          value: "{{.Values.canvasinfo.host}}"
        - name: CANVAS_INFO_BASEPATH
          value: "{{.Values.canvasinfo.basepath}}"
        ports:
        - name: {{.Release.Name}}-sqapi
          containerPort: 8080
        startupProbe:
          httpGet:
            path: /{{.Release.Name}}-{{.Values.component.name}}/tmf-api/serviceQualityManagement/v4/serviceLevelSpecification
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 30
