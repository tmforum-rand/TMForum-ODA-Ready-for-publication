apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "resourcecatalog.fullname" . }}-test-connection"
  labels:
    {{- include "resourcecatalog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "resourcecatalog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
