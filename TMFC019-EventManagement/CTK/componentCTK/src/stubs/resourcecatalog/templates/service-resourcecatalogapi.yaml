apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-resourcecatalogapi
  labels:
    app: {{.Release.Name}}-{{.Values.component.name}}
    oda.tmforum.org/componentName: {{.Release.Name}}-{{.Values.component.name}}
spec:
  ports:
  - port: 8080
    targetPort: {{.Release.Name}}-rsccatapi
    name: http-{{.Release.Name}}-resourcecatalogapi
  type: NodePort
  selector:
    impl: {{.Release.Name}}-resourcecatalogapi