apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{.Release.Name}}-serviceorderapi
  labels:
    oda.tmforum.org/componentName: {{.Release.Name}}-{{.Values.component.name}}
spec:
  replicas: 1
  selector:
    matchLabels:
      impl: {{.Release.Name}}-serviceorderapi
  template:
    metadata:
      labels:
        impl: {{.Release.Name}}-serviceorderapi
        app: {{.Release.Name}}-{{.Values.component.name}}
        version:  serviceorderapi-0.1
    spec:
      containers:
      - name: {{.Release.Name}}-serviceorderapi         
        image: {{.Values.api.image}}
        env:
        - name: RELEASE_NAME
          value: {{.Release.Name}}           
        - name: COMPONENT_NAME
          value: {{.Release.Name}}-{{.Values.component.name}}
        - name: MONGODB_HOST
          value: {{.Release.Name}}-mongodb
        - name: MONGODB_PORT
          value: "{{.Values.mongodb.port}}"
        - name: MONGODB_DATABASE
          value: {{.Values.mongodb.database}}
        - name: NODE_ENV
          value: production
        imagePullPolicy: Always  
        ports:
        - name: {{.Release.Name}}-svcordapi
          containerPort: 8080
        startupProbe:
          httpGet:
            path: /{{.Release.Name}}-{{.Values.component.name}}/tmf-api/serviceOrdering/v4/
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 30