apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-serviceorderapi
  labels:
    app: {{.Release.Name}}-{{.Values.component.name}}
    oda.tmforum.org/componentName: {{.Release.Name}}-{{.Values.component.name}}
spec:
  ports:
  - port: 8080
    targetPort: {{.Release.Name}}-svcordapi
    name: http-{{.Release.Name}}-serviceorderapi
  type: NodePort
  selector:
    impl: {{.Release.Name}}-serviceorderapi