apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "serviceorder.fullname" . }}-test-connection"
  labels:
    {{- include "serviceorder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "serviceorder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
