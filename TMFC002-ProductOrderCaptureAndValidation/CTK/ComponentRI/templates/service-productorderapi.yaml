apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-productorderapi
  labels:
    app: {{.Release.Name}}-{{.Values.component.name}}
    oda.tmforum.org/componentName: {{.Release.Name}}-{{.Values.component.name}}
spec:
  ports:
  - port: 8080
    targetPort: {{.Release.Name}}-prdordapi
    name: http-{{.Release.Name}}-productorderapi
  type: NodePort
  selector:
    impl: {{.Release.Name}}-productorderapi