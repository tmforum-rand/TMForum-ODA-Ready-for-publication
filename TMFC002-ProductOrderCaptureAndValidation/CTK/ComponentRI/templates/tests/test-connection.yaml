apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "productorder.fullname" . }}-test-connection"
  labels:
    {{- include "productorder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "productorder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
