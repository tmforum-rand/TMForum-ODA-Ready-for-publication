apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-usageapi
  labels:
    app: {{.Release.Name}}-{{.Values.component.name}}
    oda.tmforum.org/componentName: {{.Release.Name}}-{{.Values.component.name}}
spec:
  ports:
  - port: 8080
    targetPort: {{.Release.Name}}-usage
    name: http-{{.Release.Name}}-usageapi
  type: NodePort
  selector:
    impl: {{.Release.Name}}-usageapi
