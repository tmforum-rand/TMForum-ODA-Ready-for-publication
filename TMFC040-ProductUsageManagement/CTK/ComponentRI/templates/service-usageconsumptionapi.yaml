apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-usageconsumptionapi
  labels:
    app: {{.Release.Name}}-{{.Values.component.name}}
    oda.tmforum.org/componentName: {{.Release.Name}}-{{.Values.component.name}}
spec:
  ports:
  - port: 8080
    targetPort: 8080
    name: http-{{.Release.Name}}-usageconsumptionapi
  type: NodePort
  selector:
    impl: {{.Release.Name}}-usageconsumptionapi
