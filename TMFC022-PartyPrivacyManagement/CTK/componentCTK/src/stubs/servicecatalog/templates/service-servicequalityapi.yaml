apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-servicequalityapi
  labels:
    app: {{.Release.Name}}-{{.Values.component.name}}
    oda.tmforum.org/componentName: {{.Release.Name}}-{{.Values.component.name}}
spec:
  ports:
  - port: 8080
    targetPort: {{.Release.Name}}-srvqualapi
    name: http-{{.Release.Name}}-servicequalityapi
  type: NodePort
  selector:
    impl: {{.Release.Name}}-servicequalityapi