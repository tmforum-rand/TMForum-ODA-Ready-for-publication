apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-privacymanagementapi
  labels:
    oda.tmforum.org/componentName: {{.Release.Name}}-{{.Values.component.name}}
spec:
  ports:
  - name: privacymanagementapi-http
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    impl: {{.Release.Name}}-privacymanagementapi
  type: NodePort
