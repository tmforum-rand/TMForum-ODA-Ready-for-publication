apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-resourceorderingapi
  labels:
    app: {{.Release.Name}}-{{.Values.component.name}}
    oda.tmforum.org/componentName: {{.Release.Name}}-{{.Values.component.name}}
spec:
  ports:
  - port: 8080
    targetPort: {{.Release.Name}}-roapi
    name: http-{{.Release.Name}}-resourceorderingapi
  type: NodePort
  selector:
    impl: {{.Release.Name}}-resourceorderingapi
