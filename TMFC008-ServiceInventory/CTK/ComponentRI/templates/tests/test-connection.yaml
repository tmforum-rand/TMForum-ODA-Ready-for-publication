apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "serviceinventory.fullname" . }}-test-connection"
  labels:
    {{- include "serviceinventory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "serviceinventory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
