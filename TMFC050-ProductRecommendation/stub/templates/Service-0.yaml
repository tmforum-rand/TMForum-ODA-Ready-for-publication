apiVersion: v1
kind: Service
metadata:
  labels:
    oda.tmforum.org/componentName: '{{.Release.Name}}-{{.Values.component.name}}'
  name: '{{.Release.Name}}-recommendation-management-api'
spec:
  ports:
  - name: tcp-{{.Release.Name}}-recomme
    port: 8080
    targetPort: '{{.Release.Name}}-recomme'
  selector:
    impl: '{{.Release.Name}}-recommendation-management-api'
