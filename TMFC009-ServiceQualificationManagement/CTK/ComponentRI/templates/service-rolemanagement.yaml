apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-rolemanagement
  labels:
    oda.tmforum.org/componentName: {{.Release.Name}}-{{.Values.component.name}}
spec:
  ports:
  - name: rolemanagement-http
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    impl: {{.Release.Name}}-rolemanagement
  type: NodePort
