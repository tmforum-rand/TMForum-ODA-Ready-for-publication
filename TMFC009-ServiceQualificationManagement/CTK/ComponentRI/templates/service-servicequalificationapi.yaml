apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-servicequalificationapi
  labels:
    oda.tmforum.org/componentName: {{.Release.Name}}-{{.Values.component.name}}
spec:
  ports:
  - name: servicequalificationapi-http
    port: 8645
    targetPort: 8645
    protocol: TCP
  selector:
    impl: {{.Release.Name}}-servicequalificationapi
  type: NodePort
