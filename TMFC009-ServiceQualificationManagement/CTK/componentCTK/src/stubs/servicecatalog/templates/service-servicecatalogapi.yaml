apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-servicecatalogapi
  labels:
    app: {{.Release.Name}}-{{.Values.component.name}}
    oda.tmforum.org/componentName: {{.Release.Name}}-{{.Values.component.name}}
spec:
  ports:
  - port: 8080
    targetPort: {{.Release.Name}}-servcatapi
    name: http-{{.Release.Name}}-servicecatalogapi
  type: NodePort
  selector:
    impl: {{.Release.Name}}-servicecatalogapi