apiVersion: v1
kind: Service
metadata:
  labels:
    oda.tmforum.org/componentName: '{{.Release.Name}}-{{.Values.component.name}}'
  name: '{{.Release.Name}}-event-management-api'
spec:
  ports:
  - name: tcp-{{.Release.Name}}-event-m
    port: 8080
    targetPort: '{{.Release.Name}}-event-m'
  selector:
    impl: '{{.Release.Name}}-event-management-api'
